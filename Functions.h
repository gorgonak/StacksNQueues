#pragma once
#include "Node.h"

using namespace std;

void Insert();  // inserts a value into QUEUE
void Delete();  // deletes the first value of the QUEUE (FIFO)
void Display();  // displays the contents of the QUEUE

void Empty();  // checks to see if the QUEUE is empty or not.